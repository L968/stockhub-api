{
  "Kafka": {
    "BootstrapServers": "localhost:9092",
    "Consumers": {
      "OrderPlaced": {
        "Topic": "postgres.orders.order",
        "GroupId": "stockhub-orders",
        "AutoOffsetReset": "Earliest",
        "EnableAutoCommit": false
      }
    }
  },
  "ConnectionStrings": {
    "stockhub-postgresdb": "Host=localhost;Port=5432;Username=postgres;Password=root;Database=stockhub"
  }
}
